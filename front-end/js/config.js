
// Configuration & constants.

// Where the Flask back-end is running.
// If you start the server on a different port, change it here to match.
export const API_BASE = window.NRN_API_BASE || 'http://127.0.0.1:5050';

export const TOKEN_KEY = 'ncea_token'; //label written on the storage locker-> use label key to open the box
